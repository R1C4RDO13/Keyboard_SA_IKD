<?xml version="1.0" encoding="utf-8"?>
<!--
    Phase 14: background for the LEFT (‹ prev) Achievements carousel
    arrow. Flush to the screen's leading edge — the outer (start) corners
    are square; only the inner (end) corners are rounded. Solid fill is
    runtime-tinted to the primary tone in BadgeGroupAdapter so the arrow
    contrasts with the badge card behind it.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/white" />
    <corners
        android:topLeftRadius="0dp"
        android:bottomLeftRadius="0dp"
        android:topRightRadius="@dimen/card_corner_radius"
        android:bottomRightRadius="@dimen/card_corner_radius" />
</shape>
